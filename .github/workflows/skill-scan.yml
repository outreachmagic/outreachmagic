name: Skill CI

on:
  pull_request:
    paths:
      - "skills/**"
      - "scripts/**"
      - ".github/workflows/skill-scan.yml"
      - "SECURITY.md"
      - "LICENSE"
  push:
    branches: [main]

concurrency:
  group: skill-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run workspace routing tests
        run: bash scripts/run-tests.sh

      - name: Run lead-enrich tests
        run: python3 tests/test_lead_enrich.py

      - name: Run lead-email tests
        run: python3 tests/test_lead_email.py

  manifest:
    name: Update manifest
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Verify update-manifest.json is current
        run: |
          python3 scripts/generate-update-manifest.py
          git diff --exit-code skills/outreachmagic/update-manifest.json

      - name: Verify lead-enrich manifest
        run: |
          python3 scripts/generate-lead-enrich-manifest.py
          git diff --exit-code skills/lead-enrich/update-manifest.json

      - name: Verify lead-email manifest
        run: |
          python3 scripts/generate-lead-email-manifest.py
          git diff --exit-code skills/lead-email/update-manifest.json

  skill-scan:
    name: SkillScan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: HermesHub SkillScan (strict)
        run: bash scripts/skill-scan.sh --strict
