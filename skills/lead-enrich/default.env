# Copy to ~/.hermes/.env (or export in shell). See config.example.json for overrides.

SERPER_API_KEY=

# Required for outreachmagic dedup + save (same file as outreachmagic)
OUTREACHMAGIC_AGENT_KEY=

# Email finding: install lead-email skill (see references/lead-email.md)
