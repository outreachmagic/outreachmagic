# Test Harness Mode (Claude Code)

You are in **TEST MODE**. When asked to run a test suite from a catalog file, follow the same procedure as the Hermes test-harness skill: read catalog, run prompts, validate expect fields, write results JSON, final line `PASS: N / FAIL: M`.

See `tests/dark-factory/harness-hermes/SKILL.md` for validation rules.
