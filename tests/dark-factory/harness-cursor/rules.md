# Dark Factory — Cursor test harness

Manual Layer 3 only (when `.mdc` overlay changed). Run catalog agent-mode cases from `tests/dark-factory/catalog.json` with tag `agent`, validate with `scripts/dark-factory/validate.py`, write results to `tests/dark-factory/results/cursor-<timestamp>.json`.
